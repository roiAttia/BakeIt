<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_widget"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center_vertical|center_horizontal"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/colorAccent">

    <TextView
        android:id="@+id/appwidget_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/colorPrimary"
        android:fontFamily="@font/bilbo"
        android:gravity="center"
        android:padding="@dimen/widget_headline_padding"
        android:textColor="@android:color/white"
        android:textSize="@dimen/widget_headline_textsize"
        android:textStyle="bold"
        android:visibility="visible"
        tools:text="Nutella Pie" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white">

        <ListView
            android:id="@+id/appwidget_listview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/widget_listview_padding"
            android:visibility="visible" />

        <TextView
            android:id="@+id/empty_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/widget_empty_view_text"
            android:textColor="@android:color/black"
            android:textSize="@dimen/widget_empty_view_textsize" />
    </FrameLayout>

    <!--</LinearLayout>-->

</LinearLayout>